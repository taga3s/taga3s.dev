{
  "name": "@taga3s-dev/website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "test": "vp test run",
    "generate:atom": "node --import @swc-node/register/esm-register ./scripts/generate-atom.ts"
  },
  "dependencies": {
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@swc-node/register": "catalog:",
    "@swc/cli": "catalog:",
    "@swc/core": "catalog:",
    "@types/node": "catalog:",
    "jose": "catalog:",
    "wrangler": "catalog:",
    "vite-plus": "catalog:"
  }
}
