# Refer for explanation to following link:
# https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,ts,jsx,tsx,css}"
      run: pnpm format
    lint:
      glob: "*.{js,ts,jsx,tsx,css}"
      run: pnpm lint
